all:
  hosts:
    master_node:
      ansible_host: ${MASTER_NODE}
      ansible_user: ${MASTER_USER}
        # ansible_ssh_private_key_file: ${PRIVATE_KEY}
      ansible_port: ${MASTER_SSH_PORT}
    worker_node:
      ansible_host: ${WORKER_NODE}
      ansible_user: ${WORKER_USER}
        #      ansible_ssh_private_key_file: ${PRIVATE_KEY}
      ansible_port: ${WORKER_SSH_PORT}
